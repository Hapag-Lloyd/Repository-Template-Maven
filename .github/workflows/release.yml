---
name: Release

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main

jobs:
  default:
    # using a GitHub only release here. Use .github/update_workflows.sh to switch to a Maven release
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/default_release_callable.yml@219feb99199c88a43ec416656a13aed54b82e7c5
    secrets: inherit
