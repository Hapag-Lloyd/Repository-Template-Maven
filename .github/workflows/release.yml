---
name: Release

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main

jobs:
  default:
    # using a GitHub only release here. Use .github/update_workflows.sh to switch to a Maven release
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/default_release_callable.yml@709a28f7e7fdba9a6248c0003285b52786b2042e
    secrets: inherit
