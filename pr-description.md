chore: set up the `pom.xml` file

# Description

Replaces the minimal `pom.xml` and sets up a basic `README.md`.

# TODOs

- [ ] open `pom.xml` and replace all `REPLACE` placeholders with the correct values
- [ ] open `README.md` and enter a meaningful description of your project
- [ ] remove everything from the PR body

# Remarks

- specify the `<license>` used for your project
- Java version: use a LTS version
- `<groupId>` prefix is `com.hlag.whatever.you.like`. Otherwise the artifacts can't be published to Maven Central.
